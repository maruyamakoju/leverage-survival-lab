# Leverage Survival Lab — 環境変数サンプル
# 使い方: このファイルを `.env` にコピーして必要に応じて値を変更する
# `.env` 自体は .gitignore で除外されている

LSL_DEFAULT_EXCHANGE=binance
LSL_DEFAULT_SYMBOL=BTC/USDT
LSL_DEFAULT_TIMEFRAME=1h
LSL_INITIAL_EQUITY_USDT=1000000
LSL_RANDOM_SEED=42
